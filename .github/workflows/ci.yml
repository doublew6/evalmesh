name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install
        run: python -m pip install -e '.[dev]'

      - name: Install pinned Opik contract dependency
        if: matrix.python-version == '3.12'
        run: python -m pip install -e '.[opik]'

      - name: Lint
        run: ruff check src tests examples

      - name: Test
        run: python -m unittest discover -s tests -v

      - name: Privacy scan
        run: evalmesh doctor .

      - name: Synthetic smoke test
        run: |
          evalmesh validate examples/echo/evalmesh.toml
          evalmesh run examples/echo/evalmesh.toml --output .evalmesh/ci-runs.jsonl
          evalmesh monitor examples/inventory/inventory.example.json --reporter console
